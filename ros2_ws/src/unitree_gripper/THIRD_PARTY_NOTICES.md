# Third-party notices

The minimal `unitree_sdk2py` package in this directory contains files derived from Unitree Robotics' `unitree_sdk2_python` version 1.0.1. Those files remain available under the BSD 3-Clause license in `UNITREE_SDK_LICENSE`.

The bundled x86_64 server and Unitree libraries in `deps/vendor/x86_64` are built from or supplied with Unitree Robotics' `dex1_1_service` and `unitree_sdk2`. Their license texts are in `deps/vendor/x86_64/LICENSE.dex1_1_service` and `deps/vendor/x86_64/LICENSE.unitree_sdk2`.

The bundled CycloneDDS 0.10.2 installation in `deps/install/cyclonedds` retains its license and notice files under `share/doc/CycloneDDS/`.
